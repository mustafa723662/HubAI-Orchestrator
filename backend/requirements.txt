fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
python-dotenv>=1.0.0
google-genai>=1.0.0
sqlalchemy>=2.0.0
pyjwt>=2.9.0
bcrypt>=4.0.0

# Optional — uncomment to enable real calls to these providers in
# app/services/providers/. Also requires the matching *_API_KEY in .env.
# openai>=1.0.0        # OpenAI + DALL-E
# anthropic>=0.40.0    # Claude
